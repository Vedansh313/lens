# Lens local search API — Python dependencies
fastapi
uvicorn[standard]
python-multipart

# ML / search stack
torch
torchvision
faiss-cpu
numpy
pandas
pillow

# Database (PostgreSQL): ORM, migrations, driver.
# psycopg[binary] is psycopg 3 with prebuilt wheels — no local libpq/compiler
# needed on Windows. SQLAlchemy reaches it via the postgresql+psycopg:// URL.
sqlalchemy
alembic
psycopg[binary]
python-dotenv

# Auth (Phase 1): password hashing + JWT.
# bcrypt is used directly (not via passlib): passlib 1.7.4 — last released 2020 —
# fails to initialise its bcrypt backend against bcrypt 5.x, and pinning bcrypt
# back years just to keep the wrapper alive is a worse trade than calling bcrypt
# directly. PyJWT signs/verifies the access + refresh tokens. email-validator
# backs Pydantic's EmailStr in the register/login request schemas.
bcrypt
pyjwt
email-validator

# OpenAI CLIP, repackaged for pip (no git needed).
# Exposes the same `import clip` / clip.load / clip.tokenize API, so no code
# changes are required and the fine-tuned clip_finetuned.pt weights still load.
clip-anytorch
ftfy
regex
tqdm
